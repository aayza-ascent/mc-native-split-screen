# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Fabric toolchain (Minecraft 1.21.1 lane — see README "Version lanes")
minecraft_version=1.21.1
yarn_mappings=1.21.1+build.3
loader_version=0.16.14
loom_version=1.11.8

# Fabric API
fabric_version=0.116.12+1.21.1

# Mod metadata
mod_version=0.1.0
maven_group=io.ascent.nsplit
archives_base_name=native-split-screen

# Controlify (added in Phase 1 when ControllerAssigner is implemented — see README)
# controlify_version=3.0.0+lts
